# 放在项目根目录，复制为 .env 后供 docker compose 读取（勿提交）
# 本地开发仍用 web/.env，两套互不影响

NEXTAUTH_URL=http://39.96.164.140:3000
NEXTAUTH_SECRET=换成随机长字符串

# 宿主机已安装的 PostgreSQL（容器内用 host.docker.internal 访问）
DATABASE_URL=postgresql://postgres:换成服务器上的强密码@host.docker.internal:5432/wordcatch
